### Overview
- Event driven by nature
- Push-Based  

The Database can reply or connect with HTTP and makes calls when changes are made to the frontend for update call.

Ex : rethinkDB

postgres triggers and websockets also work similar to reactive databases. 

Can be used for better polling of data, instead of scaling web sockets which is considerably difficult. 

